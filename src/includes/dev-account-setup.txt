ARG dev_password

USER root

RUN apt-get update && \
    apt-get install -y sudo && \
    useradd -mU \
            -p ${dev_password} \
            -s /bin/bash \
            -G sudo \
            dev

USER dev